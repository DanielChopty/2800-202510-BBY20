<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= username %>'s Profile</title>
  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
</head>
<body class="bg-light">

  <%- include('templates/header') %>

  <div class="container my-5">
    <div class="card shadow border-0 rounded-4 p-4">
      <div class="d-flex align-items-center mb-4">
        <!-- Dynamically show uploaded image or default -->
        <img src="/<%= user && user.profilePic ? user.profilePic : 'images/default-profile.png' %>"
             alt="Profile Picture"
             class="rounded-circle me-4 border border-3"
             style="width: 120px; height: 120px; object-fit: cover; border-color: #00bfff;" />

        <div>
          <h2 class="mb-0" style="color: #003366;">Hello, <%= username %>!</h2>
          <p class="text-muted">Welcome back to your civic dashboard.</p>
          <!-- Form for user to input their feelings -->
      <form method="POST" action="/update-feelings">
        <div class="mb-3">
          <label for="feelings" class="form-label">How are you feeling?</label>
          <input type="text" class="form-control" id="feelings" name="feelings" placeholder="Enter here to receive a message!">
        </div>
        <button type="submit" class="btn btn-primary" style="background-color: #003366; color: white;">Submit</button>
      </form>

      <% if (personalizedMessage) { %>
        <div class="mt-4 alert alert-info">
          <p><strong>Personalized Message:</strong> <%= personalizedMessage %></p>
        </div>
      <% } %>
        </div>
      </div>

      <h2>User Profile</h2>

      <form action="/upload-profile-picture" method="POST" enctype="multipart/form-data">
        <input type="file" name="profilePic" accept="image/*" required>
        <button type="submit" class="btn btn-primary btn-sm ms-2" style="background-color: #003366; color: white;">Upload</button>
      </form>

      <hr>

      <p>Your insights help shape local projects. Vote on new polls, share ideas, and see feedback from your neighbors.</p>

      <a href="/polls" class="text-decoration-none">
        <div class="alert text-dark mb-4" style="background-color: #fcd34d;">
          You have <strong>3 new polls</strong> to vote on!
        </div>
      </a>

      <a href="/logout" class="btn text-white" style="background-color: #f36f21;">
        Log Out
      </a>
    </div>
  </div>

  <%- include('templates/footer') %>

  <!-- Bootstrap JS (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
